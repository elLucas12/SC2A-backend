/**
 * Assume-se que esta interface prevê que todos os métodos recebem 
 * e retornam instâncias de UsuarioModel
 */
export class IUsuariosModelRepository {
    registrar() {}
    deletar() {}
    todos() {}
}